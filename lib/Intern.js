const { Model, DataTypes } = require('sequelize');

class Intern{
    constructor(name, job, email) {
        this.name = name;
        this.id = id;
        this.email = email;
        this.job = job;
        this.email = email
    }
}

module.exports = Intern